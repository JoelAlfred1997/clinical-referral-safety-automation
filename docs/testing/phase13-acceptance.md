# Phase 13 — GitHub / CV / LinkedIn Packaging: Acceptance Record

**Date:** 2026-06-15
**Result:** ✅ PASS — the project is packaged for a public GitHub repository and for
CV / LinkedIn / interview use. The README is interview-ready (results, quick start, links),
and the interview pack ships a demo script, STAR stories, an interview Q&A, and copy-paste
CV/LinkedIn bullets. Every claim traces back to the generated evidence pack, and the packaging
is **machine-checked**: `docs/cv-linkedin/validate_pack.py` passes **5/5**.

Delivers Definition-of-Done item **11** — *"README, demo script, and CV/LinkedIn assets are
interview-ready"* — and completes the whole-project roadmap (all 13 phases done).

## What was built

`docs/cv-linkedin/`:

- **`cv-linkedin-bullets.md`** — CV entries (full / compact / one-liner), LinkedIn About,
  launch post, skills/keywords list, and a spoken elevator pitch. All figures sourced from the
  evidence pack.
- **`star-stories.md`** — seven Situation·Task·Action·Result stories (AI-out-of-the-safety-path,
  oracle-first testing, the duplicate-detection defect, the real HITL loop, BE/SE separation,
  drift-proof generated evidence, building under hard constraints), each naming its evidence
  and mapped to the behavioural prompts it answers.
- **`interview-qa.md`** — likely interview questions across architecture, AI & safety, quality &
  testing, IG/governance, and reflection — each with a grounded answer and the file to open for proof.
- **`demo-script.md`** — an 8–10 minute live-demo / screen-recording walkthrough (what to do, what
  to say, what to show), plus the headline numbers to memorise.
- **`README.md`** — index for the pack.
- **`validate_pack.py`** — the Phase 13 gate (5/5).

Top-level **`README.md`** updated: phase table marked all-13-complete, a *Results at a glance*
table, a *Quick start* block, and links to the dashboard and the interview pack.

## Acceptance gate (machine-checked)

`python docs/cv-linkedin/validate_pack.py`:

```
[PASS] Required deliverables present (5)
[PASS] Synthetic-data banner on every cv-linkedin doc
[PASS] README phase table complete (Phase 13 done, no pending rows)
[PASS] All relative links in cv-linkedin docs resolve
[PASS] Headline figures consistent with evidence pack (4)
Result: 5/5 PASS
```

The figure check is the anti-drift guard: the canonical headline numbers (15 referrals, 15/15
vs oracle, 9 created, 0 unsafe auto-creates) must appear both in the generated
`docs/testing/evidence-pack.md` *and* in the CV-facing assets that quote them — so the
portfolio bullets cannot claim a number the evidence doesn't back.

## How to reproduce

```bash
python docs/cv-linkedin/validate_pack.py     # -> 5/5 PASS
```

Read-only over the docs; creates nothing. The headline figures it checks are themselves
generated by the Phase 10/12 pipeline (`services/reporting/build_all.py`,
`services/testing/build_evidence_pack.py`).

## Scope boundary

Phase 13 is **documentation and packaging only** — no code that touches OpenMRS, no new
behaviour. The gate verifies completeness, the synthetic-data caveat, link integrity, and
figure consistency. All data synthetic; OpenMRS is a mock EPR/EMR; AI assists extraction only.

## Project complete

All 13 phases are done, each with a committed acceptance record and a passing gate. See
`docs/testing/evidence-pack.md` for the consolidated build status and
`docs/business/project-scope.md` for the Definition of Done (all 12 items met).
