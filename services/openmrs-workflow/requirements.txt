# OpenMRS referral workflow — NO third-party dependencies.
# The client and all scripts use the Python 3.9+ standard library only (urllib),
# exactly like the Phase 2 patient seeder. Nothing to install.
