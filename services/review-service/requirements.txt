# Human-in-the-loop review service — NO third-party dependencies.
# The store (sqlite3), the HTTP service (http.server) and the writeback client
# (urllib) all use the Python 3.9+ standard library only. Nothing to install.
